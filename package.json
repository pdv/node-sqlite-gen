{
  "name": "@pdv/sqlite-gen",
  "version": "1.0.0",
  "type": "module",
  "bin": {
    "sqlite-gen": "./dist/sqlite-gen.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "npx tsx test.ts",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["sqlite", "sql", "codegen", "typescript", "type-safe"],
  "author": "pdv",
  "license": "ISC",
  "description": "TypeScript code generator for type-safe SQLite queries using Node's built-in node:sqlite API",
  "devDependencies": {
    "@types/node": "^24.6.0",
    "prettier": "^3.6.2",
    "tsx": "^4.20.6",
    "typescript": "^5.9.2"
  }
}
